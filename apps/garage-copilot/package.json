{
  "name": "@deepscan/engine",
  "version": "0.1.0",
  "description": "DeepScan engine: a live OBD-II bridge (real ELM327 driver) plus diagnose / monitor / tune-advise engines for automotive diagnostics. Read-only by design; runs fully offline via a replay transport (no hardware required to build, test, or demo).",
  "type": "module",
  "bin": {
    "deepscan": "./dist/cli.js"
  },
  "main": "./dist/index.js",
  "exports": {
    ".": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/cli.ts",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.20.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.8"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
